{
  "name": "wayfarer",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@wayfarer/world-map",
    "build": "npm run build --workspace=@wayfarer/world-map",
    "start": "npm run start --workspace=@wayfarer/world-map",
    "lint": "npm run lint --workspace=@wayfarer/world-map",
    "typecheck": "npm run typecheck --workspace=@wayfarer/world-map"
  }
}
